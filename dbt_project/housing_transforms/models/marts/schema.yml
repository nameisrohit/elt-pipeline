version: 2

models:
  - name: housing_summary
    description: "Dwelling completions aggregated by area and quarter"
    columns:
      - name: area
        tests:
          - not_null
      - name: total_completions
        tests:
          - not_null
      - name: year
        tests:
          - not_null
